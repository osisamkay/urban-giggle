version: '3.8'

services:
  sharesteak-web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3500:3000"
    env_file:
      - .env
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
